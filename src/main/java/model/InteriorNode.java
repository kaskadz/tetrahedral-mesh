package model;

import org.graphstream.graph.Node;

public class InteriorNode extends NodeBase {
    protected InteriorNode(TetrahedralGraph graph, Node node, int level, String symbol) {
        super(graph, node, level, symbol);
    }

}
